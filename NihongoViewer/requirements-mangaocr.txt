# Optional: MangaOCR engine (vertical Japanese text).
#
# Heavy — pulls in PyTorch (~hundreds of MB). Only install this if you want the
# MangaOCR engine; the MeikiOCR path works without it.
#
#   pip install -r requirements-mangaocr.txt
#
# Notes:
#  - manga-ocr 0.1.14 needs transformers < 5 (the 5.x tokenizer backend breaks it).
#  - sentencepiece is required by the tokenizer under transformers 4.x.
manga-ocr>=0.1.14
transformers<5
sentencepiece>=0.2.0
